# Prompt the user to enter the first number
x = float(input("What's x? "))

# Prompt the user to enter the second number
y = float(input("What's y? "))

# Calculate the sum of the two numbers and round the result
print(round(x + y))
